# Sin PostgreSQL: usar SQLite (recomendado para desarrollo local)
USE_SQLITE=1

# Opcional: PostgreSQL (cuando USE_SQLITE no está definido o es 0)
# DB_USER=postgres
# DB_PASSWORD=tu_password
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=prognosis_db
